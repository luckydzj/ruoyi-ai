package org.ruoyi.service.coding.harness.model;

/** Coarse user-selected permission mode; tool policy still makes the final decision. */
public enum HarnessPermissionMode {
    READ_ONLY,
    WORKSPACE_WRITE,
    FULL_ACCESS
}
