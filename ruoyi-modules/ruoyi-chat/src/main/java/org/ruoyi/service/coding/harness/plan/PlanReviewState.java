package org.ruoyi.service.coding.harness.plan;

/** Human review state of the current plan revision. */
public enum PlanReviewState {
    DRAFT,
    AWAITING_APPROVAL,
    REVISION_REQUESTED,
    APPROVED
}
